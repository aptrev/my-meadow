const newCircle = {
    name: 'plot',
    shape: 'circle',
    radius: 50,
    plant: null,
    draggable: true,
}

const newRect = {
    name: 'plot',
    shape: 'rect',
    width: 100,
    height: 100,
    plant: null,
    draggable: true,
}

const newStar = {
    name: 'plot',
    shape: 'star',
    numPoints: 5,
    innerRadius: 30,
    outerRadius: 70,
    plant: null,
    draggable: true,
}

export {newCircle, newRect, newStar};